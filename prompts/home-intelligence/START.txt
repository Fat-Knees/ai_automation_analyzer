Build the advanced Home Intelligence system specified in this repository. This replaces the earlier conversational implementation plan; reconcile any existing implementation rather than starting over or discarding working code.

Read, in this order:
1. All applicable AGENTS.md instructions.
2. docs/home-intelligence/REVIEW.md.
3. docs/home-intelligence/SPEC.md, in full.
4. docs/home-intelligence/ACCEPTANCE_TESTS.md, in full.
5. docs/home-intelligence/SETUP_GUIDE.md and SOURCES.md.
6. The ignored home-intelligence.local.json, when present, without accessing secret material.
7. Existing progress, architecture, capability, and decision records.

Do not assume the rest of my ChatGPT conversation is available. Those files are the project requirements. The full advanced scope is required; phased implementation is a release strategy, not permission to quietly omit difficult features.

MISSION

Evolve my fork of ITSpecialist111/ai_automation_suggester into a local-first system that:
- recommends and safely cleans up rooms/areas/floors, device placement, names, aliases, and labels;
- classifies entity usefulness by task rather than a simplistic blacklist;
- reads real Recorder history and learns incrementally with bounded long-term memory;
- discovers contextual relationships, routines, numeric thresholds, drift, and improvement opportunities;
- understands existing automations and distinguishes explained behavior from new opportunities;
- evaluates a documented subset of proposed automations against history and in non-actuating shadow mode;
- produces evidence-backed, cost-controlled suggestions in a polished, useful Home Assistant UI;
- learns from explicit feedback and supports audited, approval-gated application and rollback.

The organization planner must explicitly recommend creating, renaming, consolidating, or splitting areas and assigning areas to physical floors. Rooms are represented by areas, not a separate nested registry. Preserve intentional exceptions. Never fabricate a room/floor location. Present unresolved questions together. Compute the effect of organization changes on existing area/floor/label-targeted actions before proposing application.

BASELINE

Inspect Git status, branches/remotes, current commit, existing instructions, README, manifest, dependencies, configuration flows, coordinator, storage, frontend assets, APIs, tests, workflows, and packaging. Run the existing test suite and record its actual results. Do not mistake mocks or a linter target for real HA compatibility.

Verify the user's fork before any GitHub write. Preserve upstream licensing and existing configurations. Work on an explicit feature branch. You may create scoped local commits and push tested, secret-scanned commits to my verified fork feature branch; do not merge, force-push, publish releases, or write to upstream.

When the configured SSH alias exists and connection access is approved, use limited read-only discovery such as ha info and ha core info. Do not probe other machines, echo credentials, read authentication files, or dump raw logs/configuration. Record the actual HA installation/version and available test tools. If access is absent, continue with synthetic fixtures and local development; do not make networking a blocker for everything.

Create/update ARCHITECTURE.md, PROGRESS.md, and CAPABILITIES.json under docs/home-intelligence/. Track every specification requirement ID against implementation paths, tests, limitations, and verification status. Record exact versions and source references for HA APIs. Keep local host details and live data out of Git.

FIRST WORKING MILESTONE

Do not stop after a plan. Implement a complete read-only organization audit slice:
- deterministic inventory and identity model;
- current and proposed floor/area tree;
- missing/duplicate/ambiguous area and floor proposals;
- device/entity assignment and naming proposals with evidence;
- before/after automation target-impact preview;
- role/usefulness explanations and user override design;
- a usable HA frontend view with review/edit/reject and clearly disabled apply controls;
- realistic synthetic examples, real HA setup/unload tests, and security tests.

AI is optional for this milestone. The deterministic audit must be useful without a provider key. Do not ship made-up confidence percentages, placeholder successes, or static UI presented as connected data.

Then continue through the remaining milestones in SPEC.md: approved mutation machinery, history and durable memory, behavioral detection, automation understanding/replay, evidence-backed AI suggestions, feedback/drift, and production hardening. Build end-to-end slices and maintain the full requirement matrix. Where practical, complete pure backend development for later stages while a production approval is pending.

ANALYTICAL REQUIREMENTS

Use provenance, actual coverage, stable entity identities, and time-versioned names/areas. Handle gaps, unavailable states, deduplication, seed states, event/attribute differences, restart races, and retention loss correctly. Never equate missing data with off or missing context with manual action.

Use episode-based support, explicitly defined denominators, matched contextual baselines, bounded candidate generation, chronological holdouts, time-aware null tests, and multiple-testing controls. Preserve interpretable evidence. Sequence mining, contextual thresholds, and drift must be evaluated against known synthetic truth, not just plausible output.

Replay must publish supported/unsupported constructs, unknown intervals, and historical match metrics. It must not call any live service or claim that observed matches prove desirability or causal savings. Full details and test gates are in the supplied files.

ACCESS AND SAFETY

Local implementation, tests, synthetic data, and approved read-only discovery are allowed now. Production deployment/restarts, live organization writes, automation writes/enabling, Recorder changes, new system installations, and paid calls require the specific approval described in AGENTS.md. Finish independent work before requesting it.

Do not present the Terminal & SSH app or AGENTS.md as a hard one-folder security boundary. Do not weaken the Codex sandbox or expose network ports to solve access problems. Never edit core .storage registry files or Recorder tables directly. Never read API tokens or private keys into the conversation. Treat names, logs, attributes, configuration text, and model output as untrusted data.

Require server-side authorization, immutable plan hashes, fresh preconditions, idempotent operation journals, target-impact acknowledgment, and conflict-aware compensation for organization writes. Record what cannot be automatically reversed. Risk follows the actual device/load, not just the domain.

TEST AND DEPLOYMENT TOOLING

Implement the tools specified in SETUP_GUIDE.md. Run fast tests locally and real HA runtime tests in a compatible isolated Linux environment or permitted CI. Do not install WSL/Docker or any other system software without approval. Test deployment and recovery against a fake remote adapter before any live attempt.

ha core check is necessary for the planned deployment flow but not sufficient. An approved deployment needs a verified artifact, completed backup, compatible behavioral-store recovery, staged files, a bounded restart budget, authenticated readiness/version/schema checks, and visible logs/diagnostics with secrets removed. No repeated restart loop and no blind database downgrade.

WORKING STYLE

Resolve ordinary reversible engineering decisions yourself. Use current official sources for changing APIs. Ask only when credentials, essential physical facts, new installations, or explicit production approval are necessary. Missing physical facts should become a compact review form, not guesses.

Use independent review or separate worktrees when available and useful, with one deployment owner. Do not weaken tests to make an implementation pass. At each milestone report working features, changed files, exact tests and outcomes, benchmark environment, known limitations, and required user action. Distinguish implemented from HA-tested and live-validated.

Start by inspecting the existing project and establishing the baseline, then implement the first working milestone. Produce actual code and tests in this session, not just another proposal.
